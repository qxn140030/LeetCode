Question: Implement int sqrt(int x).
Compute and return the square root of x.

find the last number, square of the number <= x. 
Time complexity: O(logn). 
Space complexity: O(1).

class Solution {
    public int mySqrt(int x) {
        if (x <= 1) {
            return x;
        }
        int start = 1;
        int end = x;
        while (start + 1 < end) {
            int mid = start + (end - start) / 2;
            if (mid <= x / mid) {
                start = mid;
            } else {
                end = mid - 1;
            }
        }
        return end <= x / end ? end : start;
    }
}
